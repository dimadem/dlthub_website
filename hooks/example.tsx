// Кастомный хук представляет из себя джаваскрипт - функцию,
// чье имя начинается с приставки use.
// Например, кастомный хук для феча будет называться useFetch.
// Эта функция может вызывать другие хуки(напр., useEffect или useState).